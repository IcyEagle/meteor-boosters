#!/usr/bin/env coffee
program = require "commander"

program
	.version("1.0.0")
	.command "init", "create Boosters configuration files"
	.command "setup", "configure the project in accordance with user preferences"
#	.command("list", "list packages installed", {isDefault: true})
	.parse(process.argv)
